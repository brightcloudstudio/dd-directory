services:
    _defaults:
        autowire: true
        autoconfigure: true

    Bcs\DDDirectoryBundle\EventListener\ProcessFormDataListener:
        arguments:
            $projectDir: '%kernel.project_dir%'
